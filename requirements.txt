# Rodent Study Planner — runtime dependencies
# Pinned to the versions the trained LD50 model (ml_models/ld50_model.pkl) was
# built with, so the pickle loads correctly on any machine.

Flask==3.0.0
Flask-Limiter==3.5.0
requests==2.32.5
numpy==1.26.4
pandas==2.3.3
scipy==1.11.1
statsmodels==0.14.0
scikit-learn==1.6.1
joblib==1.3.1
rdkit==2023.9.6
tenacity==8.2.2
MarkupSafe==3.0.3
reportlab==4.0.4
python-docx==0.8.11
marshmallow==3.20.1

# Authentication & access control
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3

# For production deployment (publishing):
gunicorn==21.2.0
psycopg2-binary==2.9.9    # PostgreSQL driver (Neon / Cloud SQL) — used via DATABASE_URL
